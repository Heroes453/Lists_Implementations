#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "Array_list.h"

#define DEFAULT_CAP 10
#define GROW_FACT 2

Arrlist* createlist(int capacity, int size){
	if (capacity <= 0){
		capacity = DEFAULT_CAP;
	}
}

	
Arrlist* list=(Arrlist*)malloc(sizeof
